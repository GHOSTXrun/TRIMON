import test from 'node:test';import assert from 'node:assert/strict';import {fetchCandles} from '../src/engine.mjs';
test('403 falls back to OKX and normalizes ascending OHLC without inventing bars',async()=>{const seen=[];const bars=await fetchCandles('ETH-USDT',60,180,async url=>{seen.push(url.hostname);return seen.length<=2?new Response('',{status:403}):Response.json({code:'0',data:[[120000,'10','12','9','11','4'],[60000,'9','11','8','10','3']]});});assert.deepEqual(seen,['data-api.binance.vision','api.gateio.ws','www.okx.com']);assert.equal(bars.source,'OKX');assert.deepEqual(bars[0],[60,8,11,9,10,3]);});
test('all sources failing gives an explicit error, not substitute data',async()=>{await assert.rejects(fetchCandles('ETH-USDT',60,180,async()=>new Response('',{status:403})),/公开行情暂不可用/);});
